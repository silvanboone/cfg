// Commands here will be run when you select Scout

// Reset binds
exec binds

// Winger jump script
alias +wjump "slot2; +jump; +duck"
alias -wjump "lastinv; -jump; -duck"

alias +holdWinger "slot2"
alias -holdWinger "lastinv"

// Overrides
// bind "q" "+wjump"
bind MOUSE2 +holdWinger